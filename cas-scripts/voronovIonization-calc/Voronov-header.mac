cvars : [X, Y, Z]$

polyOrderMax : 3$

fh : openw("~/max-out/VoronovModDecl.h")$

printf(fh, "#ifndef VORONOV_MOD_DELC_H ~%")$
printf(fh, "#define VORONOV_MOD_DELC_H ~%")$
printf(fh, "#include <cmath> ~%~%")$
printf(fh, "#include <algorithm> ~%~%")$
printf(fh, "extern \"C\" { ~%")$

bName   : ["Ser","Max"]$
gkVdims : [[1,2], [2], [2]]$

for bInd : 1 thru length(bName) do (

   for pi : 1 thru polyOrderMax do (
     for ci : 1 thru 3 do (
       printf(fh, "void VoronovReactRateCellAv~ax~a_P~a(const double elemCharge, const double m_, const double *vtS, const double E, const double A, const double K, const double P, const double X, double *coefIz); ~%", ci, bName[bInd], pi),
       printf(fh, "~%")      
     ),
     printf(fh, "~%")
   )

)$

printf(fh, "~% ~%")$

printf(fh, "} ~%")$

printf(fh, "#endif ~%")$

close(fh)$
